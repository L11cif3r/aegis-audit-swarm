# Talamanda Trust Layer — frontend environment template.
# Copy to `.env` (or `.env.local`) for local development.

# Backend gateway base URL.
VITE_API_URL=http://127.0.0.1:8000

# Optional API key sent as X-API-Key when the gateway has auth enabled.
VITE_API_KEY=
